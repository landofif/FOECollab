package io.github.foecollab.config;

/// Horizontal alignment for a HUD element's text (left / center / right).
/// Shared by the notification HUD and the title popups.
public enum HudAlignment {
    LEFT,
    CENTER,
    RIGHT
}
