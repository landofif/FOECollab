package io.github.foecollab.config;

/// Horizontal alignment for a HUD element (left / center / right): which point of the element
/// the configured hudX pins, and how its text lines align. Shared by all movable HUD elements.
public enum HudAlignment {
    LEFT,
    CENTER,
    RIGHT
}
